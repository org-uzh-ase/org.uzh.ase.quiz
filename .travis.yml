language: java
services:
  - docker
  - mongodb
addons:
  sonarcloud:
    organization: "org-uzh-ase"
    token:
      secure: $SONAR_TOKEN  # encrypted value of your token

before_script:
  - sleep 15
script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar sonar.host.url="https://sonarcloud.io" -Dsonar.login=$SONAR_LOGIN -Dsonar.projectKey=org-uzh-ase_org.uzh.ase.quiz

  # Docker buil check
  # - docker build -t quiz .
  # - docker run -d -p 8080:8080 quiz
  # - docker ps -a

